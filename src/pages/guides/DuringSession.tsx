import { duringSessionGuide } from '@/content/artist/during-session';
import { GuidePage } from '../GuidePage';

export default function DuringSession() {
  return <GuidePage guide={duringSessionGuide} />;
}
